import { Text, View } from 'react-native';

const index = () => {
    return (
        <View className='bg-light-background dark:bg-dark-background'>
            <Text className='mt-20 text-3xl font-bold text-light-text dark:text-dark-text'>React Native con Expo</Text>
        </View>
    );
};

export default index;